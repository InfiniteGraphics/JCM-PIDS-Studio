# AGENTS.md

本文件是给 Codex 使用的通用项目协作规则。请优先遵守本文件中的指令；如与用户后续明确要求冲突，以用户后续要求为准。

## 语言要求

- 所有回复必须使用中文。
- 代码、命令、路径、错误信息、API 名称、框架名称可以保留英文。
- 提交信息可以使用中文，除非项目已有明显英文提交风格。

## 项目背景

这是一个通用代码仓库，可能包含应用代码、脚本、配置文件、测试、文档、构建工具或自动化流程。

目标是保证改动：

- 最小化。
- 可解释。
- 可验证。
- 可回滚。
- 不触碰任务范围外的文件。
- 不引入不必要的复杂度。

## 基本工作原则

必须遵守：

- 只能修改当前仓库内、且与用户任务直接相关的文件。
- 不要修改用户未授权的系统文件、全局配置、shell 配置或其他项目文件。
- 不要在仓库外写入临时文件，除非用户明确指定。
- 不要执行会影响系统环境或其他项目的命令。
- 不要启动长期运行、资源占用较大的进程，除非用户明确要求。
- 不要擅自删除缓存、构建产物、数据文件、日志或用户生成的内容。
- 不要把密钥、密码、token、私有地址写入仓库文件或提交记录。

临时文件优先放在当前仓库下的临时目录中，例如：

```bash
./tmp
./outputs/tmp
./logs/tmp
```

使用后应清理本次任务产生的临时文件。

## 编码前先思考

不要假设，不要隐藏不确定性，不要静默选择含糊需求。

在实现前：

- 明确说明关键假设。
- 如果需求存在多种解释，先指出这些解释。
- 如果更简单的方案足够，应优先说明并采用简单方案。
- 如果需求不清楚，应停止并说明具体哪里不清楚。
- 对高风险或会影响核心逻辑、数据处理、构建流程、部署流程的改动，应先说明取舍。

对于简单、低风险、局部改动，可以直接执行，但仍需避免过度实现。

## 简单优先

只写解决当前问题所需的最小代码。

禁止：

- 添加用户没有要求的功能。
- 为一次性逻辑抽象出复杂框架。
- 添加不必要的配置项、扩展点或“未来可能用到”的灵活性。
- 为不太可能发生的场景写复杂错误处理。
- 把简单脚本改造成复杂系统。
- 擅自引入新的大型依赖。

实现前自检：

> 资深工程师会不会认为这个实现过度复杂？

如果答案可能是“会”，应简化。

## 外科手术式修改

编辑已有代码时，只改必须改的部分。

要求：

- 不要顺手“优化”无关代码。
- 不要重构无关逻辑。
- 不要修改无关格式、注释或命名。
- 不要重排 import、重格式化整文件或批量改风格，除非用户明确要求。
- 匹配项目现有风格，即使你个人会用另一种写法。
- 如果发现无关死代码、潜在 bug 或维护风险，只在回复中指出，不要擅自修改。

清理规则：

- 删除本次改动导致的未使用 import、变量、函数或类。
- 不要删除改动前已经存在的死代码，除非用户明确要求。

判断标准：

> 每一行变更都必须能直接追溯到用户的请求。

## 文件与数据规则

除非用户明确要求，否则不要修改或删除：

- 用户数据。
- 配置文件中的敏感字段。
- 日志。
- 构建产物。
- 生成文件。
- 大型二进制文件。
- 现有输出结果。
- 与当前任务无关的文档。

不要把大文件提交到 git，除非仓库明确使用 Git LFS 且用户要求。

在产生输出文件前，应确认输出路径不会覆盖已有内容。

推荐使用清晰的输出目录，例如：

```bash
outputs/task_name/
logs/task_name/
tmp/task_name/
```

## 依赖与环境规则

不要擅自修改全局环境。

优先使用项目内已有环境定义：

```bash
requirements.txt
environment.yml
pyproject.toml
package.json
pnpm-lock.yaml
yarn.lock
package-lock.json
Gemfile
go.mod
Cargo.toml
Dockerfile
```

如果确实需要新增依赖：

- 先确认项目中已有依赖是否能解决。
- 优先选择轻量、维护良好的依赖。
- 明确说明为什么需要新增依赖。
- 修改对应的项目依赖文件，而不是只在当前机器临时安装。

不要直接执行会污染全局环境的命令，例如：

```bash
pip install package
conda install package
sudo apt install package
npm install -g package
```

除非用户明确要求。

优先使用项目已有的虚拟环境、包管理器、容器或本地开发配置。

## 目标驱动执行

把任务转成可验证目标，并尽量完成验证。

示例：

- “修复 bug”

  - 定位触发条件。
  - 尽量构造最小复现。
  - 修改代码。
  - 做轻量验证。
  - 告知用户仍需确认的边界情况。

- “添加参数”

  - 确认参数含义。
  - 修改配置或命令行入口。
  - 确认默认行为不变。
  - 做 help、dry-run 或最小调用检查。

- “修改业务逻辑”

  - 明确改变的行为。
  - 保证未涉及场景不被影响。
  - 补充或更新必要测试。
  - 做最小验证。

对于多步骤任务，先给出简短计划：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

## 本地验证规则

默认只做与任务相关的轻量验证。

可优先执行：

```bash
python -m compileall .
python -m pytest
python -m pytest tests/
python script.py --help
npm test
npm run lint
npm run typecheck
npm run build
pnpm test
pnpm lint
pnpm typecheck
pnpm build
go test ./...
cargo test
```

是否执行取决于项目结构和任务范围。

允许的验证包括：

- 语法检查。
- 单元测试。
- 小型 smoke test。
- 参数解析检查。
- 配置加载检查。
- import 检查。
- 类型检查。
- lint。
- dry-run，如果项目支持。

如果验证失败，应区分：

- 代码错误。
- 依赖缺失。
- 环境缺失。
- 配置缺失。
- 测试本身不稳定。
- 路径问题。
- 当前环境不具备运行条件。

回复中必须如实说明验证结果和限制。

## Git 与提交要求

需要自动原子化提交。

每个提交应满足：

- 只包含一个逻辑改动。
- 不混合无关修复、格式化、重构和功能变更。
- 每个提交都应尽量处于可解释、可回滚状态。
- 提交信息应清楚描述变更目的。
- 提交信息为英文。

建议提交粒度：

- 一个 bug fix 一个提交。
- 一个小功能一个提交。
- 一个配置变更一个提交。
- 测试可与对应实现放在同一个提交中。
- 纯格式化只有在用户明确要求时才单独提交。

不要：

- 把多个无关修改塞进一个提交。
- 为了“顺手”清理项目而扩大 diff。
- 提交日志、大型结果文件、构建产物或敏感信息。
- 提交包含密码、token、私有路径的文件。

提交前必须尽量检查：

```bash
git status
git diff
git diff --stat
```

确认 diff 只包含本次任务相关内容。

## 安全与隐私

禁止将以下内容写入代码、配置、文档或提交记录：

- 明文密码。
- API token。
- 私钥。
- 访问密钥。
- Cookie。
- Session。
- 私有服务地址。
- 个人身份信息。
- 未授权的数据路径。

如果用户提供了密码或 token，只能用于当前会话中的必要操作，不得持久化。

## 回复格式

最终回复必须使用中文，并包含：

1. 完成了什么。
2. 修改了哪些关键文件。
3. 做了哪些验证。
4. 哪些验证无法在当前环境完成，以及原因。
5. 是否已按原子化提交处理；如果没有提交，说明原因。
6. 是否存在未解决问题或需要用户继续确认的事项。

如果有失败或不确定性，应明确说明，不要含糊带过。

## 工作方式总结

这些规则的目标是：

- 减少不必要 diff。
- 避免过度设计。
- 避免污染环境。
- 避免误改任务范围外文件。
- 保护数据和凭据安全。
- 让每次改动都可验证、可解释、可回滚。
- 保持提交原子化。